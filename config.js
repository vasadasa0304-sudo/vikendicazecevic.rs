window.SITE_CONFIG = {
  formEndpoint: "/api/inquire",
  siteSourceLabel: "website",
  mapEmbedUrl:
    "https://maps.google.com/maps?q=43.436136,19.827811&z=15&output=embed",
  // Cloudflare Web Analytics — free, privacy-first, no cookies, no consent banner.
  // Get a token: dash.cloudflare.com → Web Analytics → add site → copy the token.
  // Leave empty to disable analytics entirely (nothing external loads).
  analyticsBeaconToken: "",
};
